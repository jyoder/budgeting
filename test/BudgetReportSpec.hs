module BudgetReportSpec (spec) where

--import qualified BudgetReport
import Protolude
import Test.Hspec

spec :: Spec
spec = do
  describe "barf" $ do
    it "narf" $ do
      1 `shouldBe` (1 :: Integer)
